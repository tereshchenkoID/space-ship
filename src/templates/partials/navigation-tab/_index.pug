mixin navigation-tab(items)
  .navigation-tab&attributes(attributes)
    each item, index in items
      a(data-href=item.replace(' ', '_').toLowerCase()).navigation-tab__item(class= index === 0 && 'navigation-tab__item--active').js-navigation-tab-item= item
